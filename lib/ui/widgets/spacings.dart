import 'package:flutter/material.dart';

class Spacings {
  static SizedBox vSpace(double size) => SizedBox(
        height: size,
      );
  static SizedBox hSpace(double size) => SizedBox(
        width: size,
      );
}
